

const Props = (props) =>{
    return(
               <div>
                        
                        
         <h2>The Fullform of  JS is { props.name } {props.age}</h2>;
               </div>
    )
}

export default Props;